% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{summary.bdeb_fit}
\alias{summary.bdeb_fit}
\title{Posterior Summary for a BDEB Fit}
\usage{
\method{summary}{bdeb_fit}(object, pars = NULL, prob = 0.9, ...)
}
\arguments{
\item{object}{A \code{\link[=bdeb_fit]{bdeb_fit()}} object.}

\item{pars}{Character vector of parameter names.  Default: all model
parameters (excludes \code{log_lik}, \code{L_hat}, \code{L_rep}, \code{R_hat}, \code{R_rep},
\code{lp__}, and the internal \code{p_Am_new}).}

\item{prob}{Probability for the central credible interval. Default
0.90 (5th/95th percentiles).}

\item{...}{Ignored.}
}
\value{
A \code{posterior::draws_summary} data frame with columns
\code{variable}, \code{mean}, \code{sd}, \code{median}, two quantile columns named
by their percentile (e.g. \code{"5\%"} / \code{"95\%"} for \code{prob = 0.90}),
\code{rhat}, \code{ess_bulk}, and \code{ess_tail}.
}
\description{
Returns a tidy summary table of posterior draws for model parameters
(and optionally derived quantities), analogous to
\code{\link[stats:summary.lm]{stats::summary.lm()}} for frequentist fits.
}
\examples{
\donttest{
if (requireNamespace("cmdstanr", quietly = TRUE) &&
    nzchar(tryCatch(cmdstanr::cmdstan_path(), error = function(e) ""))) {
  data(eisenia_growth)
  dat <- bdeb_data(growth = eisenia_growth[eisenia_growth$id == 1, ])
  fit <- bdeb_fit(bdeb_model(dat, type = "individual"),
                  chains = 2, iter_warmup = 200, iter_sampling = 200,
                  refresh = 0)
  summary(fit, pars = c("p_Am", "kappa"), prob = 0.95)
}
}
}
