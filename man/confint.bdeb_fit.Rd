% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{confint.bdeb_fit}
\alias{confint.bdeb_fit}
\title{Posterior Credible Intervals for BDEB Model Parameters}
\usage{
\method{confint}{bdeb_fit}(object, parm = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{\link[=bdeb_fit]{bdeb_fit()}} object.}

\item{parm}{Character vector of parameter names.  Default: all model
parameters.}

\item{level}{Probability mass of the credible interval.  Default 0.95.}

\item{...}{Ignored.}
}
\value{
A matrix with one row per parameter and two columns named by
their percentile (e.g. \code{"2.5\%"}, \code{"97.5\%"} for \code{level = 0.95}).
}
\description{
Bayesian counterpart to \code{\link[stats:confint]{stats::confint()}}: returns the posterior
central credible interval for each model parameter.
}
\examples{
\donttest{
if (requireNamespace("cmdstanr", quietly = TRUE) &&
    nzchar(tryCatch(cmdstanr::cmdstan_path(), error = function(e) ""))) {
  data(eisenia_growth)
  dat <- bdeb_data(growth = eisenia_growth[eisenia_growth$id == 1, ])
  fit <- bdeb_fit(bdeb_model(dat, type = "individual"),
                  chains = 2, iter_warmup = 200, iter_sampling = 200,
                  refresh = 0)
  confint(fit, level = 0.90)
}
}
}
